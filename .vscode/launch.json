{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}/cmd/squash-server/main.go",
            "args": ["--cluster=debug", "--port=8012"],
            "showLog": true
        }
    ]
}